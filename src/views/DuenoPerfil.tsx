/**
 * Perfil de un dueño (mascotas + historial).
 *
 * Placeholder. Se implementa en R13 (use + Suspense) y R10 (Error Boundary).
 */

import { ViewPlaceholder } from "./ViewPlaceholder";

export function DuenoPerfil() {
  return (
    <ViewPlaceholder
      title="Perfil del dueño"
      lead="Mascotas e historial se leen con use(promise) en boundaries independientes."
      requirement="R13 · use + Suspense · R10 · Error Boundary"
    />
  );
}
