/**
 * Listado / búsqueda de dueños.
 *
 * Placeholder. Se implementa en R11 (useTransition).
 */

import { ViewPlaceholder } from "./ViewPlaceholder";

export function Duenos() {
  return (
    <ViewPlaceholder
      title="Dueños"
      lead="Buscador fluido: el input es urgente; el filtrado va en una transición."
      requirement="R11 · useTransition"
    />
  );
}
