/**
 * Formulario de nueva cita.
 *
 * Placeholder. Se implementa en R14–R16 (Actions de React 19).
 */

import { ViewPlaceholder } from "./ViewPlaceholder";

export function NuevaCita() {
  return (
    <ViewPlaceholder
      title="Nueva cita"
      lead="Agendar con useActionState, UI optimista y SubmitButton con useFormStatus."
      requirement="R14 · useActionState · R15 · useOptimistic · R16 · useFormStatus"
    />
  );
}
