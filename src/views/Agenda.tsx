/**
 * Agenda — grilla de slots por veterinario.
 *
 * Placeholder. Se implementa en R07 (useMemo / useCallback / memo).
 */

import { ViewPlaceholder } from "./ViewPlaceholder";

export function Agenda() {
  return (
    <ViewPlaceholder
      title="Agenda"
      lead="Grilla de disponibilidad por veterinario. Memoización medida con Profiler."
      requirement="R07 · useMemo / useCallback / memo · R06 · useDisponibilidad"
    />
  );
}
