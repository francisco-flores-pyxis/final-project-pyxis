/**
 * Dashboard — citas del día.
 *
 * Placeholder del andamiaje. Se implementa en R02 (useEffect + cleanup) y R08 (keys).
 */

import { ViewPlaceholder } from "./ViewPlaceholder";

export function Dashboard() {
  return (
    <ViewPlaceholder
      title="Dashboard"
      lead="Acá van las citas del día, con loading y cleanup anti-race al cambiar la fecha."
      requirement="R02 · useEffect · R08 · keys"
    />
  );
}
