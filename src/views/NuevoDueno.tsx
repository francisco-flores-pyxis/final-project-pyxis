/**
 * Alta de dueño / wizard dueño+mascota.
 *
 * Placeholder del andamiaje. Se implementa en R01 (useState) y R04 (useReducer).
 */

import { ViewPlaceholder } from "./ViewPlaceholder";

export function NuevoDueno() {
  return (
    <ViewPlaceholder
      title="Nuevo dueño"
      lead="Form controlado con validación en vivo (R01). Luego wizard de dos pasos (R04)."
      requirement="R01 · useState · R04 · useReducer"
    />
  );
}
